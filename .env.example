# Choose one installation mode, then copy the matching file to .env:
#
# Host/VM VNC:
#   cp .env.host.example .env
#
# Full Docker:
#   cp .env.full-docker.example .env
#
# This file is only here to avoid ambiguity when someone looks for .env.example.
